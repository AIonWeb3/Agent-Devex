# {{PROJECT_NAME}}

Monorepo generated by **agent-devex**.

- `contracts/agent_pay_integration` — Soroban contract with AgentPay / AgentGuard interfaces
- `agent/` — MCP server (`settle_and_execute`) bridging an LLM to Stellar

## Contract

```bash
stellar contract build --manifest-path contracts/agent_pay_integration/Cargo.toml
agent-devex deploy --project-dir . --network testnet
```

Set `STELLAR_ACCOUNT` for deploy. After deploy, set `AGENTPAY_CONTRACT_ID` for the MCP server.

## Agent

See `agent/README.md`.
